import React from 'react'

export default function AddToCard(props) {
    return (
        <div className="add">
            <form action="#" method="GET">
                <button type="button">{props.span}</button>
            </form>
        </div>
    )
}
